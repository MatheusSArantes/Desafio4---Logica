function variavel(nome, idade, parâmetros){
    console.log(`meu nome é ${nome}, tenho ${idade} anos de idade e gosto de musica ${parâmetros}`)
    }
    variavel("Matheus", 17, "indie")

    function papel(filme, parâmetros){
        console.log(`Meu filme favorito é ${filme} e eu tambem gosto de musica ${parâmetros}`)
        }
        papel("Viagem ao Centro da Terra", "indie")

        function caneta( parâmetros){
            console.log("por cada musica que eu escuto de uma banda, eu ouço " + (parâmetros * 3) + " vezes mais de outra banda" )
            }
            caneta(2)